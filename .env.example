# Supabase
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder
SUPABASE_SERVICE_ROLE_KEY=placeholder
NEXT_PUBLIC_SUPABASE_URL=placeholder

# Prelaunch Lock (Landing Page Mode)
# When true, only landing page (/) and waitlist API (/api/waitlist) are accessible
# All other routes redirect to landing page or return 404
# Set to false or remove to enable normal authentication flow
# PRELAUNCH_LOCK=true

# Redis (Optional - for production rate limiting)
REDIS_URL=redis://localhost:6379
# For Railway deployment, use: redis://redis.railway.internal:6379
# ENABLE_REDIS_RATE_LIMIT=true